{{#if searchComplete}}
{{#unless noResults}}
<div class="row search-description">
	<h6>Showing results for {{#if albumResults}}
	the album "<span class="text-capitalize">{{ searchAlbum.attributes.name }}</span>" by <span class="text-capitalize">{{ searchArtist.attributes.name }}</span>
	{{else}}the artist "<span class="text-capitalize">{{ searchArtist.attributes.name }}<span class="text-capitalize">"
	{{/if}}</h6>
</div>
{{/unless}}
{{/if}}

<div class="row search-results"></div>

{{#if searchComplete}}
{{#unless noResults}}
<div class="row load-more-container">
	<button type="button" class="btn btn-default load-more-button">
			More
	</button>
</div>
{{/unless}}
{{/if}}
